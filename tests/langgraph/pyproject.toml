[project]
name = "langgraph-showcase"
version = "0.1.0"
description = "Showcase and testing suite for LangGraph"
requires-python = ">=3.11"
dependencies = [
    "langgraph==1.2.1",
    "langchain-openai",
    "pytest",
    "python-dotenv",
]
